# Hermes Home Server — Docker Compose
# Данные Hermes монтируются в ./data (соответствует ~/.hermes внутри контейнера)

services:
  hermes:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-home
    restart: unless-stopped
    command: ["gateway", "run"]
    volumes:
      - ./data:/opt/data
      - ./backups:/opt/backups
      - ./scripts:/opt/scripts:ro
    env_file:
      - .env
    environment:
      HERMES_HOME: /opt/data
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
    healthcheck:
      # ФИКС: в образе нет wget, а порт 8642 не слушается —
      # поэтому проверяем просто, что процесс gateway жив
      test: ["CMD-SHELL", "pgrep -f 'hermes gateway' > /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
